# AETP V2 插件作者手册

**版本**：2.1
**日期**：2026-09-02
**适用版本**：AETP V2

本文是 V2 插件的实现规范。V2 插件使用 `plugin_id + version + point` 标识扩展，不使用 `task_type`、`PluginPackage`、`run.assign` 或 UI Protocol 1。旧版插件包不能直接安装到 V2 profile。

## 1. 设计边界

AETP Kernel 负责身份、项目权限、ScriptDefinition、TestTask、Run、ExecutionPlan、Resource Lease、可靠消息、Artifact 引用和状态投影。插件只实现已经发布的扩展点。

| 扩展点 | 运行位置 | 必需入口 | 典型职责 |
|---|---|---|---|
| `executor` | Master + Agent | `master`、`agent` | 解析脚本、执行 Plan、产生统一结果 |
| `resource` | Agent | `agent` | 发现、激活和释放真实硬件资源 |
| `runtime` | Agent | `agent` | 声明和检查 Python、.NET 等运行时 |
| `software` | Agent | `agent` | 检查 CANoe 等外部软件 |
| `reporter` | Master | `master` | 把执行事实转换为报告结果 |
| `analyzer` | Master | `master` | 生成统计或异常分析结果 |
| `notifier` | Master | `master` | 发送通知并返回投递状态 |
| `sharding` | Master | `master` | 按公开策略生成 Shard |
| `ui` | Web/Master | `ui` | 提供受宿主控制的配置界面 |

插件不能直接访问 SQLAlchemy、MQTT client、FastAPI `Request`、Pinia 状态或其他插件对象。需要平台能力时，使用扩展点规定的 DTO 和 Context。

## 2. 插件包结构

归档必须是 ZIP，`plugin.json` 位于根目录。入口路径相对于对应运行面目录解析：

```text
my_executor/
├── plugin.json
├── master/
│   └── entry.py
├── agent/
│   └── entry.py
├── ui/
│   ├── index.html
│   └── configuration.schema.json
└── README.md
```

不同扩展点只提供需要的目录。例如 `resource` 只需要 `agent/`，`reporter` 只需要 `master/`。入口模块不能通过绝对路径或 `..` 越出自己的目录。ZIP 路径必须是普通相对路径，不能包含绝对路径、设备名路径或符号链接逃逸。

V2 Resolver 按 Manifest 的精确入口加载模块，入口属性必须是可调用的工厂函数。工厂返回的对象必须实现该扩展点要求的方法。

## 3. Manifest

最小 executor Manifest：

```json
{
  "schema_version": 2,
  "id": "org.example.executor",
  "version": "1.0.0",
  "api_version": "2.0.0",
  "point": "executor",
  "display_name": "Example Executor",
  "entrypoints": {
    "master": "entry:create_executor",
    "agent": "entry:create_executor"
  },
  "capabilities": [
    "test.execute",
    "test.case-results"
  ],
  "static_requirements": {
    "runtimes": [],
    "software": [],
    "resources": []
  }
}
```

字段规则：

1. `schema_version` 必须为 `2`。
2. `id` 必须是全局稳定的 `PluginId`，发布后不能因为显示名称变化而改变。
3. `version` 使用 SemVer。相同 `id + version + point` 只能对应一个归档摘要。
4. `api_version` 声明插件实现的 V2 API 版本；不兼容版本必须在加载前拒绝。
5. `point` 决定允许的入口集合。`executor` 要求 Master 和 Agent 入口，`resource` 只要求 Agent 入口。
6. `capabilities` 是能力名称，不是任务类型；能力必须与实现事实一致。
7. `static_requirements` 只描述静态准入条件。节点是否可用还取决于 Agent 的动态能力快照。
8. `configuration_schema` 和 `entrypoints.ui` 使用受限相对路径；只有确实提供配置 UI 时才声明 `ui_protocol_version`。

Manifest 中的入口示例：

```json
{
  "schema_version": 2,
  "id": "org.example.resource.can",
  "version": "1.0.0",
  "api_version": "2.0.0",
  "point": "resource",
  "display_name": "Example CAN Resource",
  "entrypoints": {
    "agent": "can_provider:create_provider"
  },
  "capabilities": ["resource.can"]
}
```

## 4. Executor 扩展点

### 4.1 Master 入口

Master executor 至少提供 `parse_cases`。它接收临时脚本目录和配置，返回包含 `stable_key`、`name` 等字段的映射或对象。`stable_key` 是任务选择、分片和结果关联的稳定标识，不能依赖随机值。

```python
from collections.abc import Mapping
from pathlib import Path


class ExampleMasterExecutor:
    plugin_version = "1.0.0"

    async def parse_cases(
        self,
        script_dir: str | Path,
        configuration: Mapping[str, object],
    ) -> tuple[dict[str, str], ...]:
        del configuration
        return (
            {
                "stable_key": "tests/test_smoke.py::test_boot",
                "name": "test_boot",
                "parent_path": "tests/test_smoke.py",
            },
        )


def create_executor() -> ExampleMasterExecutor:
    return ExampleMasterExecutor()
```

Master 解析失败应抛出带有可定位信息的异常。解析器不得把硬件扫描结果伪造到脚本定义中；资源要求通过 `ExecutionRequirement` 和 Resource Provider 能力声明表达。

### 4.2 Agent 入口

Agent executor 接收 V2 `ExecutionPlan` 对应的运行上下文。Context 的公共字段包括：

| 字段/方法 | 用途 |
|---|---|
| `task_id`、`run_id`、`shard_id`、`node_id` | 关联执行事实 |
| `params` | ScriptDefinition 配置与 Shard 执行参数 |
| `script_ref` | 脚本版本、摘要、签名下载后的本地路径 |
| `case_keys` | 本 Plan 允许执行的稳定用例键 |
| `resources` | Master 已分配的资源绑定 |
| `progress(percent, stage, message)` | 上报进度 |
| `log(level, message, detail)` | 上报结构化日志 |
| `capture_log(stream, message, detail)` | 上报外部进程输出 |
| `case_status(case_key, status)` | 上报用例状态 |
| `is_cancelled()`、`raise_if_cancelled()` | 响应取消 |

一个最小 Agent executor：

```python
from collections.abc import Mapping
from typing import Any


class ExampleAgentExecutor:
    plugin_version = "1.0.0"

    async def execute(self, context) -> Mapping[str, Any]:
        await context.progress(0, "setup", "开始执行")
        await context.log("info", "执行器已启动", {"cases": context.case_keys})
        await context.raise_if_cancelled()
        for case_key in context.case_keys:
            await context.case_status(case_key, "passed")
        await context.progress(100, "done", "执行完成")
        return {"passed": True, "metrics": {"total": len(context.case_keys)}}

    async def analyze_results(self, execution_result, context) -> Mapping[str, Any]:
        del context
        return dict(execution_result)

    async def cleanup(self, context) -> None:
        del context


def create_executor() -> ExampleAgentExecutor:
    return ExampleAgentExecutor()
```

执行器应在等待、循环、外部进程读写前后检查取消，并在 `cleanup` 中释放插件自有进程和临时目录。资源 Provider 的激活和释放由 Agent Kernel 编排，executor 不应绕过 Resource Provider 直接打开硬件。

一次执行引用的是不可变 `ExecutionPlan`：其中包含精确 executor 版本、脚本摘要、用例键、资源绑定、截止时间和 Plan hash。插件不得读取“当前活动版本”来替换这些引用。

## 5. Resource Provider

Resource Provider 是 Agent 侧唯一的硬件资源边界。它必须实现：

```python
from aetp_protocol.resource import ResourceProvider


class ExampleResourceProvider:
    provider_id = "org.example.resource"
    resource_type = "example"

    def discover(self):
        return ()

    async def activate(self, binding) -> None:
        del binding

    async def deactivate(self, binding) -> None:
        del binding


def create_provider() -> ResourceProvider:
    return ExampleResourceProvider()
```

`discover()` 必须返回真实可观测的 `ResourceCapability`。CAN 通道只能来自 CAN 硬件扫描；串口 Provider 可以读取功能到端口的配置映射，但必须复核端口存在；电源 Provider 必须连接真实设备或明确的硬件 Hook。JSON 配置不能伪造 CAN 通道、电源或其他硬件存在性。

`activate`、`deactivate` 必须幂等，并在异常时抛出 Resource Provider 错误。Provider 不负责决定节点、Run 或 Lease；Master 已在 Plan 中确定资源绑定，Agent 只执行生命周期。

## 6. Reporter、Analyzer 和 Notifier

这些扩展点只运行在 Master，使用 `aetp_protocol.reporting` 中的强类型 DTO：

```python
from aetp_protocol.reporting import (
    AnalysisRequest,
    AnalysisResult,
    NotificationRequest,
    DeliveryResult,
    ReportRequest,
    ReportResult,
)


class ExampleReporter:
    async def report(self, request: ReportRequest, context) -> ReportResult:
        del context
        return ReportResult()


class ExampleAnalyzer:
    async def analyze(self, request: AnalysisRequest, context) -> AnalysisResult:
        del request, context
        return AnalysisResult(metrics={"example": 1})


class ExampleNotifier:
    async def send(self, request: NotificationRequest, context) -> DeliveryResult:
        del request, context
        return DeliveryResult(status="succeeded")
```

Reporter 产生的 `UnifiedTestResult` 不能覆盖原始 `ExecutionResult`。Analyzer 的统计结果应作为独立扩展结果保存。Notifier 返回 `succeeded`、`retrying` 或 `failed`，不得在插件内部伪造已发送事实；平台负责去重、聚合、Digest、重试和审计。

`PluginContext` 只提供受控 Artifact 读取等平台能力。大文件通过 Artifact 引用和短时签名 URL 传输，不把文件内容塞进事件、MQTT payload 或通知请求。

## 7. ScriptDefinition、TestTask 与 Run

V2 没有任务类型字段。脚本上传后由指定 executor 解析为 `ScriptDefinition`，它固定：

- `script_definition_id + revision`；
- 脚本文件名、大小和 SHA-256；
- executor 的 `plugin_id + version + archive_sha256`；
- 配置 Schema hash 和配置值；
- 稳定用例索引；
- 可选的 Execution Requirement。

TestTask 通过脚本绑定组合多个 ScriptDefinition，并声明 `parallel` 或 `sequence`、用例选择、SplitPolicy、重试策略和节点范围。Web 只保存强类型 DTO，不解析插件业务字段。

创建 Run 后，Kernel 生成不可变 Snapshot。每个脚本 Shard 都有独立 Attempt、ExecutionPlan 和 Resource Lease。Agent 只接受 `execution.plan` 与 `execution.cancel` 等 V2 消息，不接受 legacy `run.assign`。

## 8. UI 扩展点

UI 是可选的静态扩展。页面运行在宿主控制的同源 iframe 中，Manifest 可声明：

```json
{
  "entrypoints": {"ui": "index.html"},
  "configuration_schema": "ui/configuration.schema.json",
  "ui_protocol_version": 2
}
```

UI 页面不得直接访问平台 API、父页面状态或数据库。宿主应校验 `event.source`、`event.origin`、消息类型和 payload，再把结构化结果转换为 V2 请求。文件上传应交给宿主的 V2 ScriptDefinition/Artifact API，不能把未经限制的文件路径或代码传给平台。

推荐消息 envelope：

```json
{
  "type": "aetp.v2.configuration.changed",
  "payload": {
    "values": {}
  }
}
```

消息 schema、协议版本和错误码必须拒绝未知字段和不兼容版本。插件 UI 不能通过插件 ID 在宿主中增加特殊分支。

## 9. 安装、校验和版本治理

V2 插件生命周期是：

```text
上传归档 → 校验 Manifest → 计算 archive_sha256 → 保存版本 → 安装 → 管理员启用 → 重启装配 → 能力快照确认可用
```

平台保存归档摘要和 Manifest 摘要。安装成功只代表文件已就位，不代表 Agent 或 Master 已加载，也不代表节点能力 `available`。启用版本切换后，运行中的 Run 仍使用自己的精确引用；新 Run 才能使用新版本。

V2 插件中心 API：

| 操作 | API |
|---|---|
| 查看版本 | `GET /api/v2/plugins` |
| 上传归档 | `POST /api/v2/plugins` |
| 安装 | `POST /api/v2/plugins/{plugin_id}/{version}/install` |
| 启用/停用 | `POST /api/v2/plugins/{plugin_id}/{version}/enable` 或 `disable` |
| 回滚活动版本 | `POST /api/v2/plugins/{plugin_id}/{version}/rollback` |
| 删除版本 | `DELETE /api/v2/plugins/{plugin_id}/{version}` |

生产环境的写请求必须携带 `Idempotency-Key`，并由平台记录冲突和副作用。插件代码按受信任代码执行；当前进程内加载不是安全沙箱，不要把不可信插件直接安装到生产 Master/Agent。

## 10. 本地开发

安装共享协议包：

```powershell
pip install -e ..\common
```

运行 Python 检查和测试：

```powershell
..\.venv\Scripts\python.exe -m ruff check common\src master agent plugins
..\.venv\Scripts\python.exe -m pyright common\src\aetp_protocol master agent
..\.venv\Scripts\python.exe -m pytest tests -q
```

打包时确认 `plugin.json` 位于根目录，并检查归档内容：

```powershell
Compress-Archive -Path plugin.json,master,agent,ui -DestinationPath ..\example-v2.zip -Force
..\.venv\Scripts\python.exe -c "import zipfile; print(zipfile.ZipFile('..\\example-v2.zip').namelist())"
```

仓库中的 `plugins/pytest_plugin/` 提供 executor 参考实现：`plugin.json`、`master/executor.py` 和 `agent/executor.py`。它展示了 pytest 用例解析、Plan case_keys、结构化日志、JUnit Artifact 和统一结果分析，但不应被复制成新的任务类型分支。

## 11. 插件测试清单

- [ ] Manifest 的 `schema_version`、`api_version`、`point` 和入口集合合法。
- [ ] `plugin_id + version + point` 与归档内容一致，SHA-256 校验可重复。
- [ ] 所有入口路径都在对应 `master/`、`agent/` 或 `ui/` 目录内。
- [ ] Master `parse_cases` 返回稳定、非空且不重复的 `stable_key`。
- [ ] Agent executor 只执行 Plan 给出的 `case_keys`，并覆盖成功、失败、取消和超时。
- [ ] Agent executor 的 `cleanup` 在成功和异常路径都能释放进程与临时资源。
- [ ] Resource Provider 的发现结果来自真实硬件，`activate/deactivate` 可重复执行。
- [ ] Reporter 不覆盖原始执行事实，Analyzer 结果独立保存，Notifier 正确返回投递状态。
- [ ] 配置和结果 DTO 拒绝未知字段、错误版本和错误摘要。
- [ ] Artifact 只通过受控 Storage/签名 URL 读写，插件不访问平台数据库。
- [ ] 重复 Plan、重复 Artifact、重复消息和重复通知不会造成重复终态或 Lease 泄漏。
- [ ] 跨机器测试中 Agent 只通过 MQTT 和签名 HTTP URL 与 Master 交互。
