# AETP 任务类型插件开发指南

**版本**：2.0
**日期**：2026-08-26
**适用版本**：AETP v4.27 及以上

本文是当前 AETP 插件架构的开发规范。插件以 ZIP 包交付，同一个包同时包含 Master 面和 Agent 面，并且由插件自己提供脚本上传页面和任务定义配置页面。

## 1. 架构原则

一个任务类型只有一个 `task_type`。插件包包含两个运行面：

| 运行面 | 职责 | 运行位置 |
|---|---|---|
| Master 面 | 验证脚本、解析用例、生成任务定义能力、分割 Shard、计算硬件需求 | Master |
| Agent 面 | 执行 Shard、采集日志、分析结果、上报进度和结果 | Agent |

两端必须使用一致的 `task_type`、`plugin_version` 和兼容版本声明。

### 1.1 职责边界

- Master 面决定脚本是否可解析、有哪些用例、如何分割和需要哪些硬件。
- Agent 面只执行 Master 已经生成的 Shard，不重新分割、不决定资源分配。
- Master 不执行台架脚本，不直接访问测试硬件。
- Agent 只通过 `AgentTaskContext` 上报进度、日志和取消状态，不直接依赖 MQTT、FastAPI 或数据库。
- Web 主应用不应写入任何具体任务类型的字段。任务类型的配置字段、默认值、校验提示和交互全部由插件提供。

## 2. 插件包结构

```text
demo_test/
├── plugin.json
├── main.py
└── ui/
    ├── index.html             # 脚本上传/解析工作台
    ├── task-config.html       # 任务定义配置工作台
    └── vendor/                # 可选，随插件发布的前端依赖
        ├── vue.global.prod.js
        └── element-plus.js
```

`plugin.json` 和 `main.py` 必须位于 ZIP 根目录。插件 UI 必须位于 `ui/` 目录内。

ZIP 插件不需要 `pyproject.toml`，不需要 Python distribution，也不需要 entry point。固定入口是根目录的 `main.py`，且必须导出名为 `package` 的 `PluginPackage` 实例。

## 3. plugin.json

最小元数据：

```json
{
  "task_type": "demo_test",
  "plugin_version": "1.0.0",
  "display_name": "Demo 测试",
  "description": "Demo 任务类型插件",
  "entry": "main.py:package",
  "ui": {
    "entry": "index.html",
    "task_config_entry": "task-config.html",
    "protocol_version": 1,
    "min_frontend_version": "0.1.0"
  }
}
```

要求：

1. `task_type` 必须以字母开头，只能包含字母、数字、下划线、点和连字符。
2. `plugin_version` 必须与 `main.py` 中的 `PluginMetadata.plugin_version` 一致。
3. `entry` 必须是 `main.py:package`。
4. `ui.entry` 指定脚本上传页面入口。
5. `ui.task_config_entry` 指定任务定义配置页面入口。需要提供任务参数的插件必须声明它。
6. 每个声明的 UI 入口都必须存在于 `ui/` 目录内，且不能越过该目录。
7. ZIP 最大 100 MB；禁止绝对路径和包含 `..` 的路径。

`plugin.json` 只用于识别和初步检查，实际运行时元数据以 `package.metadata` 为准。两者的任务类型、版本和 UI 声明必须保持一致。

## 4. main.py 插件契约

插件只能依赖共享协议包：

```python
from aetp_protocol.capabilities import HardwareRequirements
from aetp_protocol.plugin import (
    AgentTaskContext,
    CaseInfo,
    PluginMetadata,
    PluginPackage,
    ShardSpec,
    TaskDefinitionSpec,
)
```

### 4.1 Master 面

```python
from collections.abc import Mapping
from typing import Any


class DemoMasterPlugin:
    task_type = "demo_test"
    display_name = "Demo 测试"
    plugin_version = "1.0.0"
    supported_versions = frozenset({"1.0.0"})

    # 脚本验证和解析阶段使用。
    config_schema: Mapping[str, Any] = {
        "type": "object",
        "properties": {},
    }
    upload_spec: Mapping[str, Any] = {
        "extensions": [".py", ".zip"],
        "max_size_mb": 100,
    }

    def verify_script(self, script_dir: str, config: Mapping[str, Any]) -> list[str]:
        """验证脚本目录；返回错误列表，空列表表示通过。"""
        return []

    async def parse_cases(
        self, script_dir: str, config: Mapping[str, Any]
    ) -> list[CaseInfo]:
        """解析脚本，返回稳定的用例索引。"""
        return [CaseInfo(stable_key="demo.case_1", name="Demo Case 1")]

    def build_task_definition(
        self, config: Mapping[str, Any], cases: list[CaseInfo]
    ) -> TaskDefinitionSpec:
        """描述任务定义默认用例、参数 Schema 和默认分割策略。"""
        return TaskDefinitionSpec(
            default_case_keys=tuple(case.stable_key for case in cases),
            parameter_schema=dict(self.config_schema),
            split_policy={"type": "none"},
            hardware_requirements=HardwareRequirements(),
        )

    async def split_shards(
        self,
        cases: list[CaseInfo],
        policy: Mapping[str, Any],
        config: Mapping[str, Any],
    ) -> list[ShardSpec]:
        """按任务定义配置和分割策略生成 Shard。"""
        return [ShardSpec(case_keys=tuple(case.stable_key for case in cases))]

    def result_schema(self, config: Mapping[str, Any]) -> Mapping[str, Any]:
        return {"type": "object", "required": ["status"]}

    def hardware_requirements(
        self, config: Mapping[str, Any], cases: list[CaseInfo]
    ) -> HardwareRequirements:
        return HardwareRequirements()
```

### 4.2 Agent 面

```python
class DemoAgentPlugin:
    task_type = "demo_test"
    display_name = "Demo 测试"
    plugin_version = "1.0.0"
    supported_versions = frozenset({"1.0.0"})

    # 默认值为 master。只有确实需要台架环境时才声明 agent。
    verify_location = "master"
    parse_location = "master"

    async def execute(self, context: AgentTaskContext):
        await context.progress(0, "setup", "开始 Demo 测试")
        await context.log(
            "info",
            "Demo 插件开始执行",
            {"mode": context.params.get("mode")},
        )
        await context.raise_if_cancelled()
        await context.progress(100, "done", "Demo 测试完成")
        return {"status": "passed"}

    async def cancel(self) -> None:
        pass

    async def collect_logs(self, context: AgentTaskContext) -> None:
        pass

    async def analyze_results(self, execution_result, context: AgentTaskContext):
        return {"status": execution_result.get("status", "failed")}
```

### 4.3 组装 package

```python
package = PluginPackage(
    metadata=PluginMetadata(
        task_type="demo_test",
        plugin_version="1.0.0",
        supported_versions=frozenset({"1.0.0"}),
        display_name="Demo 测试",
        config_schema=dict(DemoMasterPlugin.config_schema),
        upload_spec=dict(DemoMasterPlugin.upload_spec),
        ui={
            "entry": "index.html",
            "task_config_entry": "task-config.html",
            "protocol_version": 1,
            "min_frontend_version": "0.1.0",
        },
    ),
    master=DemoMasterPlugin(),
    agent=DemoAgentPlugin(),
)
```

## 5. 配置数据的归属

这是当前架构最重要的规则。

### 5.1 脚本配置与任务配置不同

| 数据 | 所属对象 | 用途 |
|---|---|---|
| 脚本文件、名称、任务类型 | `TestScript` | 脚本资产和用例解析 |
| `config_schema`、`upload_spec` | 插件元数据 | 描述脚本验证/解析和上传能力 |
| 任务执行参数 | `TestTask.config` | 创建任务定义时由插件 UI 编辑和保存 |
| `split_policy`、`retry_policy`、节点、优先级 | `TestTask` 平台字段 | 平台调度和可靠性控制 |
| `task_config` | `run.assign` | Master 下发给 Agent 插件的任务级配置 |
| `execution_params` | `ShardSpec` / `run.assign` | 某个 Shard 独有的执行参数 |

任务执行参数不能再放在脚本上传页面。脚本上传页面只负责选择脚本文件、填写脚本名称、请求 Master 验证和解析用例；任务参数由插件的任务配置页面提供。

### 5.2 `config_schema` 的作用

`PluginMetadata.config_schema` 是插件声明的结构化 Schema，供 Master 和插件 UI 了解配置形状。字段、默认值、校验范围和帮助文字必须由插件维护，平台 Web 不应根据 `task_type` 写条件分支。

对于复杂配置，优先使用插件自己的 `task_config_entry` 页面。Schema 可以继续作为机器可读的补充契约，但不应要求平台前端理解插件的具体字段。

## 6. 插件 UI

插件页面由 Master 从已安装插件目录安全托管，Web 主应用以同源 iframe 加载。插件页面：

- 不得直接请求 Master API。
- 不得直接访问父页面的 Vue 状态、Pinia 状态或数据库。
- 只能通过 `postMessage` 与宿主通信。
- 应接受版本化上下文，并在上下文缺少可选字段时使用自己的默认值。
- UI 中使用的 Vue、Element Plus 和图标依赖应随包放入 `ui/vendor/`，不要依赖外部 CDN。

### 6.1 脚本上传页面 `ui/index.html`

脚本上传页面通过 `aetp.plugin.context` 接收：

```json
{
  "project_id": "P-001",
  "task_type": "demo_test",
  "plugin_version": "1.0.0",
  "config_schema": {},
  "upload_spec": {
    "extensions": [".py", ".zip"],
    "max_size_mb": 100
  },
  "nodes": [],
  "verification": {
    "supported": false,
    "location": "master"
  },
  "config": {},
  "script_id": ""
}
```

选择脚本后，插件向宿主发送：

```javascript
window.parent.postMessage(
  JSON.stringify({
    type: "aetp.plugin.upload",
    payload: {
      name: "Demo 回归",
      filename: file.name,
      file: {
        __aetp_file: true,
        name: file.name,
        type: file.type,
        lastModified: file.lastModified,
        data: base64FileContent
      },
      config: {}
    }
  }),
  window.location.origin
);
```

浏览器环境中不要直接跨 iframe 传递 Vue Proxy、`File`、`Blob` 或 `ArrayBuffer`。当前协议约定使用 JSON 字符串 envelope，文件内容使用 Base64 字符串传输，宿主再还原为 `File`。

宿主回传 `aetp.plugin.upload-result`：

```json
{
  "status": "success",
  "script": {
    "script_id": "01M...",
    "parse_status": "parsed"
  }
}
```

失败时：

```json
{
  "status": "error",
  "errors": ["脚本未找到测试用例"]
}
```

### 6.2 任务配置页面 `ui/task-config.html`

任务定义页面只提供一个通用插件配置容器。具体页面由插件元数据中的 `ui.task_config_entry` 指定。

宿主发送 `aetp.task-config.context`：

```json
{
  "type": "aetp.task-config.context",
  "payload": {
    "context": {
      "project_id": "P-001",
      "task_type": "demo_test",
      "plugin_version": "1.0.0",
      "config_schema": {
        "type": "object",
        "properties": {
          "mode": {
            "type": "string",
            "enum": ["smoke", "full"],
            "default": "smoke"
          }
        }
      },
      "config": {
        "mode": "smoke"
      }
    }
  }
}
```

插件页面加载上下文后，应展示自己的字段和默认值。用户点击应用时发送：

```javascript
window.parent.postMessage(
  JSON.stringify({
    type: "aetp.task-config.config",
    payload: {
      config: {
        mode: "full"
      }
    }
  }),
  window.location.origin
);
```

宿主收到配置后只做两件事：

1. 将配置保存到 `TestTask.config`。
2. 创建 Run 时把配置放进 `run.assign.task_config`。

宿主不应读取 `mode`、`pytest_args` 或其他插件字段，也不应根据任务类型拼装配置。

### 6.3 UI 消息约定

插件发送给宿主的消息推荐使用 JSON 字符串 envelope：

```javascript
{
  "type": "aetp.task-config.config",
  "payload": {}
}
```

宿主必须校验：

- `event.source` 必须等于当前 iframe 的 `contentWindow`。
- `event.origin` 必须等于当前 Web origin。
- JSON 无法解析时直接忽略。
- `payload` 必须是对象，字段类型由插件自己校验后再发送。

宿主发送给插件的上下文也可能是对象或 JSON 字符串，插件应兼容两种形式。

## 7. 任务配置如何到达 Agent

一次 Run 触发时，Master 从任务定义读取 `TestTask.config`，插件 Master 面用它生成 Shard，然后每个 `run.assign` 都包含：

```json
{
  "task_type": "demo_test",
  "plugin_version": "1.0.0",
  "task_config": {
    "mode": "full"
  },
  "execution_params": {
    "channel": 0
  },
  "case_keys": ["demo.case_1"]
}
```

其中：

- `task_config` 是整个任务定义共享的配置。
- `execution_params` 是当前 Shard 独有的参数，由 `ShardSpec` 产生。
- Agent 执行上下文的 `params` 是两者合并结果。
- 当前合并顺序为：`task_config` 先放入，`execution_params` 后放入；同名字段由 `execution_params` 覆盖。因此插件应避免两个命名空间使用同名键。

Agent 插件通过以下方式读取配置：

```python
async def execute(self, context: AgentTaskContext):
    mode = context.params.get("mode", "smoke")
    channel = context.params.get("channel")
    await context.log("info", "开始执行", {"mode": mode, "channel": channel})
```

插件不需要解析 MQTT envelope，也不需要了解 `task_config` 的传输细节。

## 8. CaseInfo 和 ShardSpec

### 8.1 CaseInfo

```python
CaseInfo(
    stable_key="suite.open_channel",
    name="打开通道",
    parent_path="suite",
    tags=("smoke",),
    params={"channel": 0},
    estimated_duration_s=12.5,
)
```

`stable_key` 必须跨脚本版本稳定，用于任务定义勾选、分片和耗时统计。

### 8.2 ShardSpec

```python
ShardSpec(
    case_keys=("suite.open_channel",),
    execution_params={"channel": 0},
    estimated_duration_s=12.5,
)
```

优先复用 Master 的共享分割函数：

```python
from master.plugins.sharding import split_none, split_by_case_count, split_by_time
```

如果插件自定义分割，必须保证：

- 每个有效 case 至少出现在一个 Shard 中。
- 不把已删除 case 放入新 Run。
- `execution_params` 只包含当前 Shard 的差异参数。
- 分割行为由任务配置和策略决定，不由 Agent 再次修改。

## 9. AgentTaskContext

| 方法/属性 | 用途 |
|---|---|
| `context.params` | 任务级配置和当前 Shard 参数 |
| `context.script_ref` | 脚本引用、版本、哈希和本地路径 |
| `progress(percent, stage, message)` | 上报执行进度 |
| `log(level, message, detail)` | 上报结构化日志 |
| `capture_log(stream, message, detail)` | 上报外部进程输出 |
| `is_cancelled()` | 查询取消标志 |
| `raise_if_cancelled()` | 取消时抛出执行取消异常 |

长时间运行的循环、等待和外部进程操作前后都应调用取消检查。插件应在 `cancel()` 中释放自己申请的硬件、进程和临时资源。

## 10. 插件安装和版本切换

平台管理员在“插件中心”执行：

```text
上传 ZIP → 安装 → 重启 Master
```

对应 API：

| 操作 | API |
|---|---|
| 查看已加载任务类型 | `GET /api/v1/task-types` |
| 上传 ZIP | `POST /api/v1/task-types/managed` |
| 安装 | `POST /api/v1/task-types/managed/{plugin_id}/install` |
| 下载已安装 ZIP | `GET /api/v1/task-types/managed/{plugin_id}/download` |
| 启用/停用 | `PATCH /api/v1/task-types/managed/{plugin_id}?enabled=true/false` |
| 删除 | `DELETE /api/v1/task-types/managed/{plugin_id}` |
| 获取项目插件上下文 | `GET /api/v1/projects/{project_id}/task-types/{task_type}/config-context` |

插件不会热加载。安装一个启用的新版本后，平台会自动停用同一 `task_type` 的旧版本，避免两个版本同时注册同一个任务类型。必须重启 Master 后新版本才会进入运行时。

已经运行的 Run 不会因为插件升级而改变；新 Run 使用重启后加载的插件版本。

## 11. 开发和打包

安装共享协议包：

```powershell
pip install -e ..\common
```

运行插件契约测试：

```powershell
..\.venv\Scripts\python.exe -m pytest tests -q
```

打包插件：

```powershell
Compress-Archive -Path plugin.json,main.py,ui -DestinationPath ..\demo_test.zip -Force
```

检查 ZIP 根目录：

```powershell
..\.venv\Scripts\python.exe -c "import zipfile; print(zipfile.ZipFile('..\\demo_test.zip').namelist())"
```

至少应看到：

```text
plugin.json
main.py
ui/index.html
ui/task-config.html
```

## 12. pytest 插件参考

仓库中的 `plugins/pytest_plugin/` 是完整参考实现：

```text
plugins/pytest_plugin/
├── plugin.json
├── main.py
├── ui/
│   ├── index.html
│   ├── task-config.html
│   └── vendor/
└── tests/test_pytest_plugin.py
```

pytest 插件的任务配置包括 pytest 参数、Python 解释器、测试路径、收集超时、执行超时、附件路径和 fail-fast。它们全部由 `ui/task-config.html` 编辑，保存到 `TestTask.config`，再通过 `run.assign.task_config` 传给 Agent。

脚本上传页只负责：

- 选择 `.py` 或 `.zip` 脚本包；
- 填写脚本名称；
- 发送上传和解析请求；
- 展示 Master 解析结果；
- 在插件声明 Agent 验证能力时请求验证。

单个 Python 文件在 Master/Agent 侧会物化为 `test_script.py`，以满足 pytest 默认发现规则。Windows Agent 中，插件应将 `subprocess.Popen` 和阻塞读写放入 `asyncio.to_thread`，不要依赖 `asyncio.create_subprocess_exec`。

## 13. 测试清单

- [ ] `plugin.json` 与 `PluginMetadata` 的任务类型和版本一致。
- [ ] ZIP 根目录存在 `plugin.json`、`main.py`。
- [ ] 所有声明的 UI 入口都存在于 `ui/` 内。
- [ ] `verify_script` 对非法脚本返回明确错误。
- [ ] `parse_cases` 返回稳定且不重复的 `stable_key`。
- [ ] `split_shards` 覆盖全部有效 case，不重复执行整套脚本。
- [ ] 任务配置字段只出现在插件任务配置页面，不出现在平台 Web 的 `task_type` 分支中。
- [ ] 任务配置页面能读取已有 `context.config` 并发送 `aetp.task-config.config`。
- [ ] Agent `execute` 能从 `context.params` 读取任务配置。
- [ ] `analyze_results` 返回结构化结果，并覆盖成功、失败、跳过和错误场景。
- [ ] 插件处理取消、超时、外部进程失败和资源释放。
- [ ] 编写 `parse_cases`、`split_shards`、`execute`、`analyze_results` 和任务配置 UI 的回归测试。
