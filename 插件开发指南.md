# AETP 任务类型插件开发指南

> 面向插件开发者。本文说明如何实现一个「同一插件包、双端入口」的任务类型插件，
> 使其能同时被 Master（任务生成/解析/分割/硬件需求）与 Agent（执行/日志/结果/进度）发现和使用。

## 1. 核心概念

一个任务类型（如 `can_test`、`pytest`、`canoe`）只有一个插件包，包内同时提供两个入口：

| 入口 | 职责 | 运行位置 |
|---|---|---|
| **Master 面**（`MasterTaskPlugin`） | 脚本验证、用例解析、任务定义生成、Shard 分割、硬件需求计算 | Master |
| **Agent 面**（`AgentExecutionPlugin`） | 执行 Shard、采集日志、分析结果、上报进度 | Agent |

两端通过**同一个** `task_type` + `plugin_version` 关联。ZIP 插件由 Master/Agent
按固定的 `main.py` 入口加载，不依赖 Python distribution 或 `aetp.plugins` entry point。

**职责边界（D-19）**：

- Master 面是**任务生成的权威**，Agent 面是**执行的权威**。
- Agent 面**不得**生成任务定义、重新分片或决定资源分配。
- Master 面**不得**执行台架脚本或直接访问硬件。

## 2. 目录结构

推荐把插件作为独立的 ZIP 包，固定使用根目录 `main.py` 作为入口：

```text
demo_test/
├── plugin.json              # task_type / plugin_version
├── main.py                  # 固定入口，导出 package
├── ui/
│   └── index.html            # 插件配置页面入口（必须随插件包发布）
└── resources/               # 可选的非 UI 资源
```

## 2.1 从零创建一个可上传 ZIP 插件包

下面以 `demo_test` 为例，创建一个同时包含 Master 面和 Agent 面的 ZIP 插件包。
命令默认在 Windows PowerShell 中执行。

### 步骤 1：创建目录和虚拟环境

```powershell
mkdir demo_test
cd demo_test
New-Item plugin.json -ItemType File
New-Item main.py -ItemType File
```

插件开发环境需要安装共享协议包和打包工具：

```powershell
pip install -e ..\common
```

如果插件项目不在仓库根目录，请把 `..\common` 改为实际的
`AutomotiveElectronicsTestPlatform\common` 路径。

### 步骤 2：创建 ZIP 插件文件

ZIP 插件不需要 `pyproject.toml`、wheel 或 Python entry point。固定约定如下：

```text
demo_test.zip
├── plugin.json        # 插件身份和版本元数据
├── main.py            # 固定入口，必须导出 package
├── ui/index.html       # 插件配置网页入口
└── resources/         # 可选：脚本模板、Schema、静态资源等
```

`plugin.json`：

```json
{
    "task_type": "demo_test",
    "plugin_version": "1.0.0",
    "display_name": "Demo 测试"
}
```

要求：

1. `plugin.json` 和 `main.py` 必须位于 ZIP 根目录。
2. `task_type` 只能使用字母开头的字母、数字、下划线、点和连字符。
3. `plugin_version` 必须与 `main.py` 中 `PluginMetadata.plugin_version` 一致。
4. ZIP 最大 100 MB；禁止绝对路径和 `..` 路径，防止路径穿越。
5. `main.py` 必须导出名为 `package` 的 `PluginPackage` 实例。
6. `ui/index.html`（或 `PluginMetadata.ui.entry` 指定的入口）必须存在于 `ui/` 目录内；没有插件配置网页的任务类型不能在 Web 上传脚本。

### 步骤 3：编写最小可运行插件

`main.py`：

```python
from typing import Any, Mapping

from aetp_protocol.capabilities import HardwareRequirements
from aetp_protocol.plugin import (
    CaseInfo,
    PluginMetadata,
    PluginPackage,
    ShardSpec,
    TaskDefinitionSpec,
)


class DemoMasterPlugin:
    task_type = "demo_test"
    display_name = "Demo 测试"
    plugin_version = "1.0.0"
    supported_versions = frozenset({"1.0.0"})
    config_schema: Mapping[str, Any] = {
        "type": "object",
        "properties": {"message": {"type": "string"}},
    }
    upload_spec: Mapping[str, Any] = {"extensions": [".py"], "max_size_mb": 20}

    def verify_script(self, script_dir: str, config: Mapping[str, Any]) -> list[str]:
        return []

    async def parse_cases(self, script_dir: str, config: Mapping[str, Any]) -> list[CaseInfo]:
        return [CaseInfo(stable_key="demo.case_1", name="Demo Case 1")]

    def build_task_definition(
        self, config: Mapping[str, Any], cases: list[CaseInfo]
    ) -> TaskDefinitionSpec:
        return TaskDefinitionSpec(
            default_case_keys=tuple(case.stable_key for case in cases),
            parameter_schema=dict(self.config_schema),
            split_policy={"type": "none"},
        )

    async def split_shards(
        self,
        cases: list[CaseInfo],
        policy: Mapping[str, Any],
        config: Mapping[str, Any],
    ) -> list[ShardSpec]:
        return [ShardSpec(case_keys=tuple(case.stable_key for case in cases))]

    def result_schema(self, config: Mapping[str, Any]) -> Mapping[str, Any]:
        return {"type": "object", "required": ["status"]}

    def hardware_requirements(
        self, config: Mapping[str, Any], cases: list[CaseInfo]
    ) -> HardwareRequirements:
        return HardwareRequirements()


class DemoAgentPlugin:
    task_type = "demo_test"
    display_name = "Demo 测试"
    plugin_version = "1.0.0"
    supported_versions = frozenset({"1.0.0"})
    verify_location = "master"
    parse_location = "master"

    async def execute(self, context):
        await context.progress(0, "run", "开始 Demo 测试")
        await context.log("info", "Demo 插件开始执行")
        await context.raise_if_cancelled()
        await context.progress(100, "done", "Demo 测试完成")
        return {"status": "passed"}

    async def cancel(self) -> None:
        pass

    async def collect_logs(self, context) -> None:
        pass

    async def analyze_results(self, execution_result, context):
        return {"status": execution_result.get("status", "failed")}


package = PluginPackage(
    metadata=PluginMetadata(
        task_type="demo_test",
        plugin_version="1.0.0",
        supported_versions=frozenset({"1.0.0"}),
        display_name="Demo 测试",
        config_schema=dict(DemoMasterPlugin.config_schema),
        upload_spec=dict(DemoMasterPlugin.upload_spec),
    ),
    master=DemoMasterPlugin(),
    agent=DemoAgentPlugin(),
)
```

生产插件应为 `execute`、`analyze_results`、`parse_cases` 和 `split_shards` 编写契约测试，
并根据实际任务实现脚本验证、用例索引、硬件需求、日志采集和结构化 case 结果。

### 步骤 4：打包 ZIP 并检查入口

在包含 `plugin.json` 和 `main.py` 的目录执行：

```powershell
Compress-Archive -Path plugin.json,main.py,resources -DestinationPath demo_test.zip -Force
```

也可以使用 Python 打包，适合脚本化构建：

```powershell
\.venv\Scripts\python.exe -c "import zipfile; z=zipfile.ZipFile('demo_test.zip','w',zipfile.ZIP_DEFLATED); z.write('plugin.json'); z.write('main.py'); z.close()"
```

检查 ZIP 根目录：

```powershell
\.venv\Scripts\python.exe -c "import zipfile; print(zipfile.ZipFile('demo_test.zip').namelist())"
```

输出必须至少包含：

```powershell
plugin.json
main.py
```

### 步骤 5：通过 Web 上传、安装和加载

启动 Master 后，使用平台管理员登录 Web：

```text
插件中心 → 上传 ZIP → 安装 → 停止/启用（按需）→ 重启 Master
```

也可以直接调用 API：

```text
POST /api/v1/task-types/managed
Content-Type: multipart/form-data
file=<demo_test.zip>
```

上传成功后，生命周期记录保存在 Master 运行目录的：

```text
master/plugins/archives/
master/plugins/manifest.json
master/plugins/packages/
```

安装接口会把 ZIP 解压到 Master 运行目录的受控插件目录，并立即校验 `main.py` 导出的
`PluginPackage`。由于插件是 Python 可执行代码，当前实现**不会热加载**；必须重启 Master，
启动时加载已安装且启用的 ZIP 插件。
重启后可在“插件中心”确认任务类型、版本和 Agent 执行面已经加载。

### 步骤 6：验证插件是否可用于运行

验证顺序建议如下：

1. `GET /api/v1/task-types` 能看到 `demo_test`。
2. Agent 注册日志中的 `plugin_versions` 包含 `demo_test: 1.0.0`。
3. 创建或上传该任务类型脚本，Master 能完成 `verify_script` 和 `parse_cases`。
4. 触发 Run，Master 生成 Shard 并下发 `run.assign`。
5. Agent 能执行 `execute`，并上报 progress、log、result。
6. Run 详情显示成功状态和结构化结果。

### 插件生命周期 API

| 操作 | API | 说明 |
|---|---|---|
| 查看已上传包 | `GET /api/v1/task-types/managed` | 仅平台管理员 |
| 上传 ZIP | `POST /api/v1/task-types/managed` | 仅 `.zip`，最大 100 MB |
| 安装 | `POST /api/v1/task-types/managed/{plugin_id}/install` | 安装到 Master 当前 Python 环境 |
| 启用/停用 | `PATCH /api/v1/task-types/managed/{plugin_id}?enabled=true/false` | 重启后生效 |
| 删除 | `DELETE /api/v1/task-types/managed/{plugin_id}` | 必须先停用；同时卸载 distribution |

插件上传、安装、启用、停用和删除都需要平台管理员权限。停用只影响 Master 重启后的加载；
已经运行中的任务不会被强制中断。

## 3. 共享契约（`aetp_protocol`）

插件依赖 `aetp_protocol` 包中的共享契约，**不要** import Master/Agent 的内部模块。

关键类型：

```python
from aetp_protocol.plugin import (
    CaseInfo,            # 解析出的用例索引项
    ShardSpec,           # 分割产出的 Shard
    TaskDefinitionSpec,  # 任务定义快照
    PluginMetadata,      # 元数据
    PluginPackage,       # 双端入口容器
    AgentPackageSpec,    # Agent 执行包分发元数据（可选）
)
from aetp_protocol.capabilities import HardwareRequirements  # 硬件需求
```

### 3.1 CaseInfo

```python
CaseInfo(
    stable_key="case_open_channel",      # 跨版本稳定标识（必填，pytest nodeid/CANoe 用例名/cdd 服务路径）
    name="打开 CAN 通道",                 # 显示名
    parent_path="suite",                 # 父路径/分组
    tags=("can",),                       # 标签
    params={"channel": 0},               # 用例参数
    estimated_duration_s=12.5,           # 预估耗时（秒，可选；by_time 分割依据）
)
```

> `stable_key` 必须跨版本稳定，用于版本 diff 与任务定义勾选引用。

### 3.2 ShardSpec

```python
ShardSpec(
    case_keys=("case_a", "case_b"),      # 本 Shard 负责的 case 集合
    execution_params={"channel": 0},     # 每 Shard 专属执行参数（Agent execute 用）
    estimated_duration_s=30.0,           # 预估耗时（可选）
)
```

## 4. 实现 Master 面

继承（或实现）`MasterTaskPlugin` 协议：

```python
from typing import Any, Mapping
from aetp_protocol.plugin import CaseInfo, ShardSpec, TaskDefinitionSpec
from aetp_protocol.capabilities import HardwareRequirements

class CanTestPlugin:
    task_type = "can_test"
    display_name = "CAN 通信测试"
    plugin_version = "1.0.0"
    supported_versions = frozenset({"1.0.0"})
    config_schema: Mapping[str, Any] = {"type": "object", "properties": {...}}
    upload_spec: Mapping[str, Any] = {"extensions": [".py"]}

    def verify_script(self, script_dir: str, config: Mapping[str, Any]) -> list[str]:
        # 上传后、解析前快速失败：返回错误列表，空 = 通过
        return []

    async def parse_cases(self, script_dir: str, config: Mapping[str, Any]) -> list[CaseInfo]:
        # 解析脚本产出用例索引，stable_key 必须稳定
        return [CaseInfo(stable_key="case_1", name="Case 1")]

    def build_task_definition(
        self, config: Mapping[str, Any], cases: list[CaseInfo]
    ) -> TaskDefinitionSpec:
        return TaskDefinitionSpec(
            default_case_keys=tuple(c.stable_key for c in cases),
            parameter_schema=dict(self.config_schema),
            split_policy={"type": "by_case_count", "cases_per_shard": 10},
        )

    async def split_shards(
        self, cases: list[CaseInfo], policy: Mapping[str, Any], config: Mapping[str, Any]
    ) -> list[ShardSpec]:
        # 复用共享分割函数（见 §6）
        return split_by_case_count(cases, cases_per_shard=int(policy.get("cases_per_shard", 10)))

    def result_schema(self, config: Mapping[str, Any]) -> Mapping[str, Any]:
        return {"type": "object", "required": ["passed", "case_results"]}

    def hardware_requirements(
        self, config: Mapping[str, Any], cases: list[CaseInfo]
    ) -> HardwareRequirements:
        # 强类型硬件需求（§18.5），供节点匹配
        return HardwareRequirements()
```

## 5. 实现 Agent 面

继承（或实现）`AgentExecutionPlugin` 协议：

```python
from typing import Any, Mapping
from aetp_protocol.plugin import AgentTaskContext

class CanTestAgent:
    task_type = "can_test"
    plugin_version = "1.0.0"
    supported_versions = frozenset({"1.0.0"})
    display_name = "CAN 通信测试"
    # 可选：台架侧预检/解析能力声明（默认 "master"）
    verify_location = "master"
    parse_location = "master"

    async def execute(self, context: AgentTaskContext) -> Any:
        await context.progress(0, "setup", "初始化")
        await context.log("info", "开始执行", {"channel": context.params.get("channel")})
        # ... 执行台架脚本 ...
        await context.raise_if_cancelled()  # 长循环/等待前后检查取消
        await context.progress(100, "done", "完成")
        return {"status": "passed"}

    async def cancel(self) -> None:
        # 释放硬件、保存取消原因（可选）
        pass

    async def collect_logs(self, context: AgentTaskContext) -> None:
        # 采集/整合外部进程日志（可选）
        pass

    async def analyze_results(
        self, execution_result: Any, context: AgentTaskContext
    ) -> Mapping[str, Any]:
        # 分析执行结果，产出结构化 case 结果（CANoe 类在结束后调用）
        return {"passed": execution_result.get("status") == "passed"}

    # 可选：台架侧脚本预检/解析（仅 verify_location/parse_location == "agent" 时生效）
    def verify_script(self, script_dir: str, config: Mapping[str, Any]) -> list[str]:
        return []

    def parse_cases(self, script_dir: str, config: Mapping[str, Any]) -> list["CaseInfo"]:
        return []
```

### 5.1 AgentTaskContext 可用方法

| 方法 | 用途 |
|---|---|
| `progress(percent, stage, message)` | 进度上报 |
| `log(level, message, detail)` | 任务日志（`(run_id, sequence)` 幂等） |
| `capture_log(stream, message, detail)` | 采集外部进程日志流 |
| `is_cancelled()` | 是否已请求取消 |
| `raise_if_cancelled()` | 已取消则抛 `ExecutionCancelled` |

> 插件只能通过这些方法上报，**不得**直接依赖 MQTT、FastAPI 或数据库。

## 6. 复用共享分割函数（P6.3）

Master 面的 `split_shards` 可以直接复用 `master.plugins.sharding` 提供的纯函数：

```python
from master.plugins.sharding import split_none, split_by_case_count, split_by_time
```

| 函数 | 策略 | 关键参数 |
|---|---|---|
| `split_none(cases)` | 不分割，单 Shard | — |
| `split_by_case_count(cases, cases_per_shard=N)` | 每 N 个 case 一个 Shard | `cases_per_shard` |
| `split_by_time(cases, target_duration_s=T, default_duration_s=60)` | 按耗时切到目标时长 | `target_duration_s`、`default_duration_s` |

D-21 语义已内建：`by_time` 缺耗时的 case 用 `default_duration_s` 兜底参与切片，单 case 超限不丢失。

## 7. 组装 PluginPackage 并注册

```python
from aetp_protocol.plugin import PluginMetadata, PluginPackage, AgentPackageSpec

package = PluginPackage(
    metadata=PluginMetadata(
        task_type="can_test",
        plugin_version="1.0.0",
        supported_versions=frozenset({"1.0.0"}),
        display_name="CAN 通信测试",
        config_schema={...},
        upload_spec={...},
        # 可选：Agent 执行包动态分发（Agent 缺失/版本不符时下载安装）
        agent_package=None,
    ),
    master=CanTestPlugin(),
    agent=CanTestAgent(),
)
```

### 7.1 ZIP 入口声明

ZIP 插件不声明 `pyproject.toml` 或 Python entry point。`plugin.json` 声明插件身份，
根目录 `main.py` 导出 `package`，Master 与 Agent 启动时加载该入口并分别使用其中的
Master 面和 Agent 面。

### 7.2 可选：Agent 执行包动态分发

如果 Agent 侧插件不能随 Agent 一起发布（需要 Master 按需下发），在 `PluginMetadata.agent_package` 填入：

```python
AgentPackageSpec(
    package_name="aetp-plugin-can-test",
    version="1.0.0",
    download_url="https://.../aetp-plugin-can-test-1.0.0.zip",
    sha256="<64位十六进制>",
    entry_point="main:package",  # ZIP 固定入口；保留字段仅用于兼容协议
)
```

约束：`agent_package.version` 必须等于 `plugin_version`。

## 8. 插件版本兼容策略

- 升级插件后需在 `supported_versions` 中声明兼容范围，否则旧脚本无法被解析/执行（`PLUGIN_VERSION_MISMATCH`）。
- `script_cases` 与解析实现版本强绑定，插件升级后对存量脚本重新解析需显式触发 `parse`。

## 9. 开发检查清单

- [ ] Master 面与 Agent 面的 `task_type` / `plugin_version` 一致
- [ ] `stable_key` 跨版本稳定
- [ ] `verify_script` 上传后、解析前快速失败
- [ ] `split_shards` 复用共享分割函数（或自定义 `custom` 策略）
- [ ] `hardware_requirements` 返回强类型 `HardwareRequirements`
- [ ] Agent 面只通过 `AgentTaskContext` 上报，不 import MQTT/DB/FastAPI
- [ ] ZIP 根目录存在 `plugin.json` 和 `main.py`
- [ ] `main.py` 导出有效的 `PluginPackage` 实例 `package`
- [ ] `supported_versions` 含当前 `plugin_version`
- [ ] 为 `parse_cases` / `split_shards` / `execute` / `analyze_results` 编写契约测试

## 10. 项目内 pytest 插件示例

仓库根目录已提供可运行的 pytest 插件示例：

```text
plugins/pytest_plugin/
├── plugin.json
├── main.py
├── README.md
└── tests/test_pytest_plugin.py
```

该插件的任务类型为 `pytest`，版本为 `1.0.0`，能力包括：

- Master 侧检查测试目录，调用 `pytest --collect-only -q` 解析用例；
- Master 侧按 `cases_per_shard` 生成 Shard；
- Agent 侧执行 pytest，实时采集输出日志；
- 生成 JUnit XML 报告；
- 解析通过、失败、错误、跳过和耗时等 case 级结果；
- JUnit XML 中的每个 case 的 stdout/stderr 和跳过原因可在 Web 展开查看；
- 通过 `artifact_paths` 配置把脚本目录下的报告、截图或其他文件上传到 Master。

Windows Agent 为兼容 MQTT 使用 Selector 事件循环，Agent 插件不得直接调用
`asyncio.create_subprocess_exec`；需要把 `subprocess.Popen` 和阻塞式读写放入
`asyncio.to_thread`，并在取消时终止子进程。单个 `.py` 脚本下载到 Agent 后会
物化为临时目录中的 `test_script.py`，以便 pytest 默认收集规则识别。

pytest 默认使用 `--capture=tee-sys -o log_cli=true -o junit_logging=all`，因此
脚本的 `print`、`logging` 和 JUnit case 输出都会被采集。配置示例：

```json
{
    "pytest_args": ["-q"],
    "artifact_paths": ["reports/*.html", "screenshots/**/*.png"]
}
```

报告和附件在 Run 结束时上传到 Master 的产物区；Web 的 Run 详情页显示运行日志、
可展开的 case 输出和产物下载。跳过用例及 xfail 不会单独导致 Run 失败，只有失败/错误
用例或 pytest 非零退出码会导致 Run 失败。

任务类型插件的配置网页必须随 ZIP 包发布在 `ui/` 目录中，Web 只负责以 iframe
宿主加载，不把 task_type 的配置字段写进平台前端。插件页面通过 `postMessage`
接收 Master 提供的配置 Schema、项目节点能力和验证接口上下文，并回传配置与验证请求。
插件可在 `PluginMetadata.ui` 中声明入口和协议版本：

```python
ui={"entry": "index.html", "protocol_version": 1, "min_frontend_version": "0.1.0"}
```

插件页面还必须自己提供脚本上传控件。Master 通过配置上下文传入 `upload_spec`，插件据此设置文件扩展名和大小提示；用户选择文件和名称后，页面通过同源 `postMessage` 发送：

```javascript
window.parent.postMessage({
    type: "aetp.plugin.upload",
    payload: { name, filename: file.name, file, config }
}, window.location.origin)
```

宿主回传 `aetp.plugin.upload-result`，成功载荷包含 `script`；插件拿到 `script.script_id` 后才允许执行 Agent 验证。页面不应直接调用 Master API。

在插件目录运行契约测试：

```powershell
..\.\.venv\Scripts\python.exe -m pytest tests -q
```

构建项目插件 ZIP：

```powershell
Compress-Archive -Path plugin.json,main.py,ui -DestinationPath ..\pytest_plugin.zip -Force
```

然后在 Web 的“插件中心”上传 `plugins/pytest_plugin.zip`，安装后重启 Master。
Agent 执行节点必须预先安装 pytest 及测试脚本所需的依赖。

## 11. 全链路 pytest 脚本示例

项目内提供端到端验证脚本：

```text
plugins/pytest_plugin/examples/e2e_script/
├── test_aetp_e2e.py
├── pytest.ini
└── README.md
```

它覆盖脚本上传、用例收集、分片下发、Agent 执行、日志输出、JUnit XML
结果分析和 Master 结果投影。默认场景包含通过、参数化、跳过、预期失败和耗时用例。
如需验证失败 Run，在 Agent 运行环境设置 `AETP_E2E_INCLUDE_FAILURE=1`。

脚本包打包命令：

```powershell
cd plugins/pytest_plugin/examples/e2e_script
Compress-Archive -Path test_aetp_e2e.py,pytest.ini -DestinationPath ..\aetp_pytest_e2e_script.zip -Force
```

该 ZIP 是测试脚本包，不是插件包，因此不包含 `plugin.json` 或 `main.py`；上传后任务类型选择
`pytest`。详细观察点和本地验证命令见该目录的 `README.md`。
